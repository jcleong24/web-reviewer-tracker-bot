package com.modefair.webreviewer;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Placeholder unit test so {@code mvn test} passes without external services.
 *
 * <p>A full {@code @SpringBootTest} context-load test requires a reachable
 * PostgreSQL and the datasource env vars; add one under a test profile
 * (e.g. Testcontainers) once the persistence layer is in place.
 */
class WebReviewerApplicationTests {

    @Test
    void placeholder() {
        assertTrue(true);
    }
}
